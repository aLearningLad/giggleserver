{
  "name": "giggleserver",
  "version": "1.0.0",
  "description": "My little RESTful jokes API. A quick refresher on Express.js, it has no database. Jokes are stored in a hard-coded array of joke objects. Each joke has an ID to allow for fetching a specific joke. Might extend it later with a postgreSQL database and simple auth, then toss it onto Render or Railway.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongodb": "^6.17.0",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
